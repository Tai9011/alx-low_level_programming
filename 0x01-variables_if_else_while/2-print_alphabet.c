#include<stdio.h>
/**
 * main - main function
 * A function that prints the alphabet
 * in lower case
 * Return: always returns 0 (success)
 */
int main(void)
{
	int ch = "abcdefghijklmnopqrstuvwxyz\n";
	putchar(ch);
	return (0);

}

