#include "main.h"
/**
 * _abs - prints absolute value
 * @nb: variable
 * Return: always returns 0
 */
int _abs(int)
{
	if (int >= 0)
		_putchar(int);
	else
	{
		int nb2 = int * -1;

		_putchar(nb2);
	}
	return (0);
}
